# Knowtify — Project Rules

## Git
- Never run git commands (commit, push, add, etc.)
- Never mention commits or ask about pushing
- The user handles all git operations

## Commit messages
- Never suggest or write commit messages
- Never mention Cursor or any IDE name in any code, comments, or messages
